import cellTypes from "../cell-types/cellTypes";

const cellsGenerator = () => {
    const { 
        empty,
        grass,
        sand,
        water,
        wall,
        goal
    } = cellTypes
}

export default cellsGenerator;
